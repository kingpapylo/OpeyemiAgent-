# mob_clipboard plugin

## Purpose
Clipboard read/write for OpeyemiAgent (ports MobileClaw clipboard skill).

## Local Contracts
- Uses _a0_connector host-bridge to reach the phone clipboard.
- Write is non-destructive to other apps; user-initiated only.
